import { makeStyles } from '@material-ui/core/styles';

const GameStyle = makeStyles({
  root: {
    display: 'flex',
    alignItems: 'center',
    flexDirection: 'column'
  }
});

export default GameStyle;
